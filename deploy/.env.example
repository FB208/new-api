# DEPLOY_DIR 下的 .env 示例（与 docker-compose.yml 同目录）
#
# 这个文件只负责告诉 compose 该拉哪个镜像，两行都由 GitHub Actions 在每次部署时
# 自动写入，正常情况下不需要手工维护。首次手动执行 docker compose 时可以先照抄一份。
#
# 注意：这里不是应用的环境变量文件。new-api 自身的配置（SQL_DSN、REDIS_CONN_STRING、
# SESSION_SECRET 等）仍然在 /home/DockerVolumes/new-api/.env，由 compose 的 env_file 加载。

REGISTRY_REPO=docker.example.com/new-api
VERSION=v1.2.3
